/**
 * Represents a Steam ROM Manager-compatible JSON output for
 * writing to manual JSON manifests
 */
interface ShortcutExportData {
	title: string;
	target: string;
}

export default ShortcutExportData;
